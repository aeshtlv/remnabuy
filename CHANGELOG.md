# Changelog - Config & Debug Improvements

## [2026-01-07] - Исправление чтения настроек из .env

### Изменено

#### `src/config.py`
- ✅ Удалены лишние отладочные принты из валидаторов
- ✅ Убран циклический импорт `src.utils.logger` 
- ✅ Добавлено кеширование настроек через `_settings_cache`
- ✅ Улучшен валидатор `parse_internal_squads` - теперь всегда читает напрямую из `os.getenv()`
- ✅ Добавлено детальное логирование настроек при запуске:
  - Цены подписок (Stars)
  - UUID сквадов (external/internal)
  - Список админов
- ✅ Добавлен параметр `reload=True` в `get_settings()` для принудительной перезагрузки

#### `src/services/api_client.py`
- ✅ Добавлено логирование payload в `create_user()` - теперь видно, какие данные отправляются в API
- ✅ Добавлено логирование payload в `update_user()` - для отладки применения сквадов

### Добавлено

#### `test_settings.py`
- ✅ Тестовый скрипт для локальной проверки чтения настроек из `.env`
- ✅ Показывает цены подписок, UUID сквадов, список админов
- ✅ Сравнивает значения Settings с переменными окружения

#### `ENV_DEBUG.md`
- ✅ Подробная инструкция по отладке настроек
- ✅ Что проверить в логах
- ✅ Как проверить в панели Remnawave
- ✅ Решения типичных проблем

#### `DEPLOY_INSTRUCTIONS.md`
- ✅ Пошаговая инструкция по развёртыванию обновлений на сервере
- ✅ Как проверить `.env` файл
- ✅ Как читать логи бота
- ✅ Диагностика проблем с ценами и сквадами

### Удалено
- ❌ `GIT_PULL_FIX.md` - больше не нужен, конфликты разрешены
- ❌ `fix_git_pull.sh` - больше не нужен

## Следующие шаги

1. **На сервере:**
   ```bash
   cd /opt/remnabuy
   git pull
   docker compose build --no-cache bot
   docker compose up -d
   docker compose logs -f bot
   ```

2. **Проверить логи:**
   - Блок "SETTINGS LOADED" должен показать правильные значения из `.env`
   - При создании пользователя должны быть логи "🔵 API: Creating user with payload"

3. **Протестировать:**
   - Активировать пробную подписку
   - Проверить в панели Remnawave, что сквады установлены
   - Попробовать оплату и проверить цены

4. **Если всё работает:**
   ```bash
   rm test_settings.py ENV_DEBUG.md DEPLOY_INSTRUCTIONS.md CHANGELOG.md
   ```

## Исправленные проблемы

- ✅ Цены подписок не читались из `.env` (использовались дефолтные)
- ✅ UUID сквадов не читались из `.env` (были пустые)
- ✅ Циклический импорт между `config.py` и `logger.py`
- ✅ Отсутствие логирования для отладки API вызовов
- ✅ Git конфликты на сервере (`<<<<<<< Updated upstream`)

