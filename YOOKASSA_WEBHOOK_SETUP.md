# Настройка Webhook для YooKassa

## Что такое Webhook?

Webhook - это автоматические уведомления от YooKassa о статусе платежей. Когда пользователь оплачивает заказ, YooKassa отправляет POST запрос на ваш сервер, и платеж обрабатывается автоматически без необходимости нажимать кнопку "Проверить статус".

## Настройка

### 1. Настройка в .env файле

Убедитесь, что в вашем `.env` файле указаны следующие параметры:

```env
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_secret_key
YOOKASSA_WEBHOOK_URL=https://shftvpn.click/webhook/yookassa
YOOKASSA_WEBHOOK_PORT=8080
```

### 2. Настройка домена и портов

#### Вариант A: Использование Nginx (рекомендуется)

Если у вас есть Nginx, настройте проксирование:

```nginx
server {
    listen 80;
    server_name shftvpn.click;

    location /webhook/yookassa {
        proxy_pass http://localhost:8080/webhook/yookassa;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Важно: передаем оригинальное тело запроса для проверки подписи
        proxy_set_header Content-Length $content_length;
        proxy_set_header Content-Type $content_type;
        proxy_pass_request_body on;
    }
}
```

#### Вариант B: Прямое открытие порта

Если вы хотите открыть порт напрямую (не рекомендуется для продакшена):

1. Убедитесь, что порт 8080 открыт в файрволе
2. Настройте проброс порта в вашем роутере/облачном провайдере

### 3. Настройка в личном кабинете YooKassa

1. Войдите в [личный кабинет YooKassa](https://yookassa.ru/my)
2. Перейдите в раздел **"Настройки"** → **"HTTP-уведомления"**
3. В поле **"URL для HTTP-уведомлений"** укажите:
   ```
   https://shftvpn.click/webhook/yookassa
   ```
4. Выберите события, на которые нужно отправлять уведомления:
   - ✅ `payment.succeeded` (обязательно)
   - ✅ `payment.canceled` (опционально)
5. Нажмите **"Сохранить"**

### 4. Проверка работы

После настройки:

1. Перезапустите бота
2. В логах вы должны увидеть:
   ```
   🌐 YooKassa webhook server started on port 8080
   📡 Webhook URL: https://shftvpn.click/webhook/yookassa
   ```

3. Создайте тестовый платеж через бота
4. После оплаты платеж должен обработаться автоматически (без нажатия "Проверить статус")

### 5. Проверка подписи

YooKassa отправляет подпись в заголовке `X-YooMoney-Signature`. Бот автоматически проверяет подпись для безопасности. Если подпись не совпадает, запрос будет отклонен.

## Troubleshooting

### Webhook не работает

1. **Проверьте доступность URL:**
   ```bash
   curl https://shftvpn.click/webhook/yookassa
   ```
   Должен вернуть `{"status":"ok"}` или ошибку 404 (если сервер не запущен)

2. **Проверьте логи бота:**
   - Должны быть сообщения о запуске webhook сервера
   - При получении webhook должны быть логи обработки

3. **Проверьте настройки Nginx:**
   - Убедитесь, что проксирование настроено правильно
   - Проверьте, что порт 8080 доступен для Nginx

4. **Проверьте файрвол:**
   - Порт 8080 должен быть открыт (или проксироваться через Nginx)

### Платежи не обрабатываются автоматически

1. Убедитесь, что `YOOKASSA_WEBHOOK_URL` указан правильно в `.env`
2. Проверьте, что в личном кабинете YooKassa указан правильный URL
3. Проверьте логи бота на наличие ошибок при обработке webhook'ов

### Ошибка "Invalid signature"

Это означает, что подпись от YooKassa не совпадает. Проверьте:
- Правильность `YOOKASSA_SECRET_KEY` в `.env`
- Что Nginx правильно передает тело запроса (не изменяет его)

## Безопасность

- Webhook endpoint защищен проверкой подписи от YooKassa
- Используйте HTTPS для webhook URL (обязательно для продакшена)
- Не открывайте порт 8080 напрямую в интернет без Nginx/прокси

## Альтернатива: Polling (без webhook)

Если вы не можете настроить webhook (нет домена, проблемы с настройкой), пользователи могут проверять статус платежа вручную через кнопку "Проверить статус" в боте. Это менее удобно, но работает без дополнительной настройки.

