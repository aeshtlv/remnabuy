# Инструкции по развёртыванию обновлений

## Что было исправлено

### 1. Упрощена и улучшена система конфигурации

- **Убраны** лишние отладочные принты и циклические импорты из `src/config.py`
- **Добавлено** кеширование настроек для производительности
- **Улучшен** парсинг `DEFAULT_INTERNAL_SQUADS` - теперь всегда читает напрямую из переменных окружения
- **Добавлено** детальное логирование при загрузке настроек (цены, сквады, админы)

### 2. Добавлено логирование API вызовов

- В `src/services/api_client.py` добавлены логи для `create_user()` и `update_user()`
- Теперь можно видеть, какие именно данные отправляются в Remnawave API
- Логи помогут понять, применяются ли сквады на стороне API или проблема в боте

### 3. Создан тестовый скрипт

- `test_settings.py` - для локальной проверки чтения настроек из `.env`
- Показывает цены, сквады, админов
- Можно запустить на сервере для диагностики

## Что делать на сервере

### Шаг 1: Обновить код

```bash
cd /opt/remnabuy
git stash  # если есть локальные изменения
git pull
```

### Шаг 2: Проверить .env файл

```bash
cat .env
```

**Убедитесь, что все необходимые переменные заполнены:**

```bash
# Токены и URL
BOT_TOKEN=ваш-токен
API_BASE_URL=https://your-remnawave-api.com
API_TOKEN=ваш-api-токен

# Админы (через запятую, если несколько)
ADMINS=ваш-telegram-id

# Цены подписок в Stars
SUBSCRIPTION_STARS_1MONTH=150
SUBSCRIPTION_STARS_3MONTHS=400
SUBSCRIPTION_STARS_6MONTHS=700
SUBSCRIPTION_STARS_12MONTHS=1200

# Сквады для новых пользователей
DEFAULT_EXTERNAL_SQUAD_UUID=ваш-внешний-squad-uuid
DEFAULT_INTERNAL_SQUADS=внутренний-uuid-1,внутренний-uuid-2,внутренний-uuid-3

# Пробный период (дней)
TRIAL_DAYS=3

# Язык по умолчанию
DEFAULT_LOCALE=ru

# Уровень логирования
LOG_LEVEL=INFO
```

**ВАЖНО для `DEFAULT_INTERNAL_SQUADS`:**
- Разделяйте UUID запятыми: `uuid1,uuid2,uuid3`
- Можно с пробелами после запятой: `uuid1, uuid2, uuid3`
- **НЕ добавляйте пробелы ПЕРЕД запятой**

### Шаг 3: Пересобрать и запустить бот

```bash
docker compose build --no-cache bot
docker compose up -d
docker compose logs -f bot
```

### Шаг 4: Проверить логи

В логах бота должен появиться блок:

```
=============================================================
SETTINGS LOADED
=============================================================
Subscription prices (Stars):
  1 month:  150 stars (env: 150)
  3 months: 400 stars (env: 400)
  ...

Squads configuration:
  External squad: ваш-uuid (env: ваш-uuid)
  Internal squads: ['uuid1', 'uuid2'] (env: uuid1,uuid2)
  Parsed internal squads count: 2
  ...
=============================================================
```

**Проверьте:**
- Значения из столбца "stars" совпадают с `(env: значение)`
- `Internal squads count` > 0 (если вы указали внутренние сквады)
- `External squad` не `None` (если вы указали внешний сквад)

### Шаг 5: Протестировать бот

1. **Откройте бот в Telegram**
2. **Нажмите** кнопку "Пробная подписка"
3. **Активируйте** пробную подписку

**В логах должны появиться записи:**

```
Creating trial user for 12345678: external_squad=ваш-uuid, internal_squads=['uuid1', 'uuid2'] ...
🔵 API: Creating user with payload: {...}
Trial user created successfully: abc-123
🔵 API: Updating user abc-123 with payload: {...}
Applied squads on trial user abc-123: ...
```

4. **Откройте панель Remnawave**
5. **Найдите** созданного пользователя
6. **Проверьте**, что сквады установлены правильно

### Шаг 6: Протестировать оплату

1. **Откройте бот в Telegram**
2. **Нажмите** "Купить подписку"
3. **Выберите** период (например, 1 месяц)
4. **Проверьте**, что цена отображается правильно (из вашего `.env`)

**Ожидаемое поведение:**
- Цена в окне оплаты должна совпадать с `SUBSCRIPTION_STARS_1MONTH` из `.env`
- После оплаты в логах должны быть записи об установке сквадов (как для триала)

## Если что-то не работает

### Проблема 1: Цены не обновились

**Проверьте:**
```bash
docker compose logs bot | grep "SETTINGS LOADED" -A 20
```

Если видите `(env: NOT SET)` - значит переменные не читаются из `.env`.

**Решение:**
1. Проверьте, что в `.env` нет лишних пробелов или кавычек
2. Перезапустите контейнер: `docker compose restart bot`

### Проблема 2: Сквады не применяются

**Проверьте логи:**
```bash
docker compose logs bot | grep "🔵 API:"
```

**Если в payload есть сквады, но в панели Remnawave их нет:**
- Возможно, API Remnawave игнорирует эти параметры
- Попробуйте установить сквады вручную через панель
- Проверьте версию API Remnawave

**Если в payload НЕТ сквадов:**
- Проверьте `.env` файл
- Запустите `python3 test_settings.py` на сервере
- Проверьте формат `DEFAULT_INTERNAL_SQUADS` (должны быть запятые без пробелов перед ними)

### Проблема 3: Бот не запускается

**Проверьте логи:**
```bash
docker compose logs bot --tail 100
```

**Если есть SyntaxError:**
- Проверьте, что нет конфликтов Git (маркеры `<<<<<<<`, `=======`, `>>>>>>>`)
- Запустите: `git status` и разрешите конфликты

**Если есть ImportError:**
- Пересоберите контейнер: `docker compose build --no-cache bot`

## Очистка после отладки

Когда всё заработает и вы убедитесь, что настройки читаются корректно, можно удалить тестовые файлы:

```bash
cd /opt/remnabuy
rm test_settings.py
rm ENV_DEBUG.md
rm DEPLOY_INSTRUCTIONS.md
git add -A
git commit -m "Remove debug files"
```

## Дальнейшие улучшения

После того, как базовая функциональность заработает:

1. **Реферальная система** - уже реализована, но требует тестирования
2. **Промокоды** - уже реализованы, можно создавать через админку
3. **Уведомления** - настроить `NOTIFICATIONS_CHAT_ID` и `NOTIFICATIONS_TOPIC_ID` в `.env`
4. **Дополнительные периоды подписки** - можно добавить 2 месяца, 9 месяцев и т.д.
5. **Статистика** - добавить команды для админа для просмотра статистики продаж

